
# 본문의 글은 인프런 앨런 Swift문법 마스터 스쿨(온라인) 부트캠프의 강의 영상을 본후 공부한 내용을 정리하여 기록한것입니다.
https://inf.run/YyoR

```swift


//비동기 Async 개념
//일을 시작시키고 작업이 끝날때 까지 "안기다린다" => 메인쓰레드가 다른 일 처리를 시작할 수 있다.


//동기 Sync
//일을 시작시키고 뿐만아니라 작업이 끝날때 까지 "기다린다 => 메인쓰레드가 다른 일 처리를 시작할 수 없다.

//비동기라는 개념이 필요한 이유는 대부분은 서버와의 통신 때문인다.

//직렬 Serial 처리
//다른 하나의 쓰레드에 일을 보내는 것
//메인쓰레드에서 분산처리 시킨 작업을 "다른 한개의 쓰레드에서" 처리한다.
//순서가 중요한 작업을 처리할때 사용


//동시 Concurrent 처리
//몇개의 쓰레드로 분산할지는 시스템이 알아서결정하지만 여러개의 쓰레드에 일을 보내는것
//메인쓰레드에서 분산처리 시킨 작업을 "다른 여러개의 쓰레드에서" 처리한다.
//각자 독립적이지만 작업의성경이라던가 등 유사한 여러개의 작업을 처리할때 사용한다.

//동시처리가 좋아보이는데 왜 직렬 처리가 필요할까 => 작업에 순서가 필요할수도 있기 때문
//당근마켓에서 중고물품사진이 주르륵 뜨는거 그런게 유사한 작업을하는 어떤걸 처리하는걸 =? 동시 처리

//비동기란말과 동시란말이 같은말인가?
//비동기라는 말은 작업을 보내는 쓰레드에 관련된 개념 => 기다릴지 말지
//동시처리라는 말은 메인쓰레드에서 다른 쓰레드로 작업을 보낼때에 개념 => 일을 한개로보내냐 여러개로 보내냐

//왜 동시성 프로그래밍이 필요할까?
//앱에서 인스타그램같은 사진이 엄청 많은 화면을 위아래로 스크롤하면 버벅거리는데
//그이유가 앱이 표시하는 화면에 관여해서 메인쓰레드에서만 시키고 있기때문에 버벅이는것이다.
//앱을 실행흘 한뒤 메인런루프(무한반복)을 한다. 메인런루프는 이벤트(터치등등)을 받고 처리를한다.이 과정이 1초에 35억 번한다.
//그리고 그걸 메인쓰레드가 아이폰 화면에 1초에 60번 띄어주는 그려주는일을 한다. 정리하면
//1. 앱이시작될때 앱을 담당하는 메인 런루프를 생성한다.
//2. 메인런루프는 이벤트처리를담당하고 => 이벤트를 통해 어떤 함수를 실행할지 선택/실행한다
//3. 이 함수의 실행의 결과를 화면에 보여준다 => 화면을 다시 그린다(1초에60번)
//4. 이 일을 메인쓰레드가 한다. 그래서 항상 메인쓰레드는 좀 바쁘다
//그래서 보통 네트워크와 관련된 일들은 메인쓰레드와 분리시켜서 다른 쓰레드에서 시킨다.왜냐 네으퉈크작업은 오래걸리는작업이기떄문이다.
//메인쓰레드에서 서버에서 뭔가 데이터도받아와야하고 화면을 다시그려야되고 하면 다시그리는일 1초에 60번 사이에 서버를 받아오는 시간으로인해 느려지는것.
//결론은 결국 앱의 최적화를 위한 개념이다.

//개발자는 작업(task)를 대기행렬(Queue)에 보내기만 하면 ios에서 알아서 여러쓰레드로 일을 나눠준다(분산처리,동시처리)
//큐 는 작업이 들어오면 바로바로 다른쓰레드로 일을 보낸다.

//그래서 우리는 작업을 이제 큐로 보내기만하면 된다.

//큐에는 디스패치큐,오퍼레이션 큐가 있는 우리는 여기서 디스패치큐에 관한내용만 공부할것이다.
//오퍼레이션 큐에 관한내용은 나중에 꼭 인프런 앨런강의를 따로 들을것